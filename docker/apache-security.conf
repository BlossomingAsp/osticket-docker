# Apache hardening for osTicket. Copied into the image and enabled at build
# time (see Dockerfile). osTicket serves the web root from /var/www/html and
# relies on its own .htaccess, so only these minimal security tweaks apply.
ServerTokens Prod
ServerSignature Off
